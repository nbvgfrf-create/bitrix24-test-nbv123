services:
  bitrix-import:
    build: .
    container_name: bitrix-import
    restart: unless-stopped
    environment:
      TZ: Europe/Moscow
    volumes:
      - ./:/app
    working_dir: /app
    command: ["php", "-S", "0.0.0.0:8080", "-t", "/app"]
    ports:
      - "8080:8080"
